// Copyright (C) 2026 Ilya I. Nikitin <ilya.i.nikitin@proton.me>
// SPDX-License-Identifier: GPL-3.0-or-later

#import "lib.typ": *
#import "../slides-speech.typ": note

== Введение

#set text(size: 16pt)

- Классический фильтр Калмана рекурсивный алгоритм оценки вектора $x$ состояния
  линейной динамической системы по зашумлённым наблюдениям.
- Применяется в навигации, управлении, обработке сигналов; цена ошибки в таких
  системах высока.
- Каждое свойство фильтра оформлено как теорема и проверено системой
  интерактивного доказательства теорем.
- Инструменты: #Rocq, MathComp (алгебра и анализ), Coq-Quantum (алгебра
  эрмитовых матриц), infotheo (теория вероятностей), CoqEAL (извлечение
  эффективного кода).

#v(6pt)

#grid(
  columns: (1fr, 1fr, 1fr),
  stat(
    [#rocq-theory-statements()],
    [определений и утверждений],
    value-size: 30pt,
  ),
  stat([#rocq-theory-loc-bound()], [строк формализации], value-size: 30pt),
  stat(
    [#rocq-theory-admitted()],
    [незавершённых доказательств],
    color: unn-colors.green,
    value-size: 30pt,
  ),
)

#note("intro")

== Цель и задачи

#set text(size: 15pt)

*Цель:* формализовать дискретный фильтр Калмана, доказать его основные свойства
и извлечь исполняемый алгоритм, связанный со спецификацией.

#v(2pt)

+ Формализовать базовую теорию матриц, порядок Лёвнера, спектральную теорему.
+ Определить шаги фильтра, обосновать структуру ковариаций.
+ Доказать несмещённость оценки и оптимальность усиления Калмана.
+ Формализовать наблюдаемость, управляемость, детектируемость, двойственность.
+ Решение ДАУР: существование, единственность, устойчивость, сходимость.
+ Извлечь исполняемый код, провести численные эксперименты и построить
  иллюстрации.

#pause
#v(8pt)

#results-progress()

#note("goals")
