#import "../lib.typ": *

#part_count.step()

= Извлечение исполняемого алгоритма <sec:extraction>

Формальная верификация полезна тогда, когда её артефакты применимы на практике:
между доказанным и работающим кодом должен существовать однозначный мост. В этой
главе описана реализованная стратегия получения исполняемого кода фильтра
Калмана и решателя ДАУР из формализации в #Rocq, а также её связь с
визуализациями основных результатов (разделы @sec:dare-frob, @sec:riccati-mono).

В отличие от ручного переноса, где соответствие спецификации и кода проверяется
глазами, реализованный подход делает это соответствие _машинно-проверяемым_:
исполняемая программа записана на слое вычислимых матриц `seqmx` библиотеки
#link(
  "https://github.com/coq-community/coqeal",
)[CoqEAL] и связана с абстрактным определением из `theories/kalman.v` теоремой
уточнения (`refines`). После этого тот же терм извлекается штатным механизмом
`Extraction` #Rocq в код на #OCaml. Таким образом исполняемый код доказанно
совпадает со спецификацией, не переопределяя фильтр заново.

Рассматривались два подхода.

+ _Программное извлечение в C++_ через #link(
    "https://github.com/bloomberg/crane",
  )[bloomberg/crane], экспериментальный экстрактор #Rocq $arrow$ C++. Пробное
  исследование показало, что crane порождает чистый современный C++ (например,
  примитивную арифметику `PrimFloat` $arrow$ `double`), однако на момент работы
  он фиксирует версию #Rocq 9.0.x и потому несовместим с проектом (#Rocq 9.1 +
  MathComp 2.5 + mathcomp-analysis). Кроме того, поддержка зависимых типов и
  иерархии HB в MathComp у crane не продемонстрирована.

+ _Уточнение через CoqEAL и штатное извлечение в #(OCaml)_. Именно этот подход
  реализован и используется. Доказательства используют зависимые матрицы
  `'M[R]_(m, n)`, а исполнение ведётся на представлении `seqmx` (списки
  списков). Каждая `*_seqmx`-программа связана с абстрактным определением
  теоремой `refines`, после чего механизм `Extraction` порождает идиоматичный
  #OCaml. Коэффициентное поле остаётся _точным_ (рациональные числа) без
  округления до `double`; в коде программы нет `Obj.magic`.

== Постановка задачи извлечения <sec:extract-scope>

Извлечению подлежат _структурно вычислимые_ определения из #raw(
  "theories/kalman.v",
): шаг предсказания/обновления ковариации, усиление Калмана и полный шаг Риккати
`riccati_step` (см. разделы @sec:kf-step, @sec:dare-existence), а также
производные от них эксперименты: степень матрицы, грамианы наблюдаемости и
управляемости (`theories/obsv_bound.v`) и матрица замкнутого контура (#raw(
  "theories/dare.v",
)).

Не извлекаются:

- все теоремы (`unbiased`, `kalman_gain_optimal`, #raw(
    "dare_stabilizing_sol_frob",
  ) и др.) суть _сертификаты корректности_; их вычислительное содержание
  отсутствует в исполняемом выходе;
- доменные гипотезы (`Q_psd`, `R_pd`, `Exp_*`) суть предпосылки без
  вычислительного значения;
- стационарное решение `Pss` целиком: оно определено как #emph[топологический
    предел] (`mx_mono_lim`, раздел @sec:mono-limits) и невычислимо как таковое;
  на практике его заменяет итерация `riccati_step` до неподвижной точки.

== Конкретизация полиморфного поля `numClosedFieldType` <sec:extract-field>

Формализация в `theories/kalman.v` полиморфна по типу скаляров
`C : numClosedFieldType`. Для исполняемого кода поле необходимо зафиксировать;
рассмотренные кандидаты: `algC` (точно, но непрактично медленно),
`complex realType` (требует моста к `numClosedFieldType`) и машинный `double`.

Реализованный подход обходит дилемму на другом уровне. Программа `seqmx`
параметризована _слоем операций_ CoqEAL (`zero_of`, `one_of`, `add_of`,
`mul_of`, `inv_of`, ...) без обращения к структуре поля. Один и тот же терм
поэтому исполняется на любом типе с такими операциями: на `rat` (эталон через
`vm_compute`), на `bigQ` (двоичная арифметика `Bignums`, для параметричности на
стороне доказательств) и на zarith `Q.t` в #OCaml (быстрый рантайм). Поскольку
модели фильтра в иллюстрациях вещественны и рациональны, извлечённое ядро
считает _точно_, без ошибок округления. Числа с плавающей точкой появляются лишь
как производные _презентационные_ величины (собственные значения, полуоси
эллипсов, нормы, расстояния), вычисляемые в тонком драйвере исключительно для
построения графиков.

Этот приём состоит в отображении `numClosedFieldType` на сопряжение
`conj : R -> R`: обобщённые определения из `theories/riccati_def.v`
(`predict_cov`, `update_cov`, ...) параметризованы произвольным `conj`, и при
`conj := conjC` дословно совпадают со спецификацией над `numClosedFieldType`, а
при `conj := idfun` дают вещественный исполняемый случай. Так выглядит
исполнимый полушаг предсказания над слоем операций (`hmul_op`, `add_seqmx`,
`ctr_seqmx`):

#rocq-snippet("seqmx/riccati_seqmx.v", "Definition predict_cov_seqmx ")

Полный исполнимый шаг Риккати складывается из таких же полушагов:

#rocq-definition("seqmx/riccati_seqmx.v", "riccati_step_seqmx")

Соответствие спецификации устанавливается теоремой уточнения: при
`conj := conjC` исполнимый шаг вычисляет в точности `riccati_step` из
`theories/kalman.v` на любой матрице, связанной со своим `seqmx`-представлением
отношением `Rseqmx`.

#rocq-corollary(
  "seqmx/riccati_seqmx.v",
  "kalman_riccati_step_seqmx_correct",
  proof: false,
)

Конкретизация на поле `rat` позволяет исполнять программу прямо внутри #Rocq
через `vm_compute` и сверять результат со спецификацией. Для скалярной системы
$F = 2$, $G = H = Q = R = P_0 = 1$ шаг итерации задаётся подстановкой
коэффициентов:

#rocq-snippet("seqmx/riccati_seqmx.v", "Definition ex_step ")

Два шага скалярного ДАУР из $P_0 = 1$ дают $5\/6$, затем $13\/16$; значение
вычислено `vm_compute` и связано со спецификацией леммой `ex_iter_correct`:

#rocq-snippet("seqmx/riccati_seqmx.v", "Definition ex_two ")
#rocq-snippet("seqmx/riccati_seqmx.v", "Lemma ex_two_val ", proof: false)

== Обращение матрицы <sec:extract-types>

В спецификации `invmx` определён через формулу Крамера
(`(\det A)^-1 *: \adj A`). CoqEAL не предоставляет уточнения `invmx` над
произвольным полем, поэтому обращение инновационной ковариации вынесено в
параметр `cinv` с обязательством `cinv_correct` (`refines (invmx S) (cinv sS)`).

Обязательство снимается для любого числа выходов $p$ единой исполнимой
программой `cinv_fl` (метод Фаддеева-Леверье) над слоем операций CoqEAL. Она
вычисляет обратную матрицу рекуррентой по следу:
$
  M_1 = E, quad c_(n-j) = - "tr"(A M_j) / j, quad
  M_(j+1) = A M_j + c_(n-j) E, quad A^(-1) = - c_0^(-1) M_n.
$

Корректность опирается на два факта о присоединённой матрице характеристической
матрицы $G(x) = "adj"(x E - A)$ и характеристическом многочлене
$chi = "char_poly" A$, доказанных с нуля в `theories/faddeev.v`. Тождество
Крамера $(x E - A) G(x) = chi(x) E$ покоэффициентно даёт рекурренту для матриц
коэффициентов $G$ и равенство $c_k = chi_k$. Тождество Якоби
$chi'(x) = "tr"(G(x))$, отсутствующее в MathComp и CoqEAL, даёт формулу следа
$"tr"(A M_j) = - j chi_(n-j)$, используемую исполняемой рекуррентой.

#rocq-lemma("faddeev.v", "char_poly_deriv", proof: false)

Сама рекуррента и обратная матрица записываются на абстрактном поле буквально по
формулам выше:

#rocq-definition("faddeev.v", "flM")

#rocq-definition("faddeev.v", "fl_inv")

Итоговая теорема устанавливает совпадение с `invmx` на обратимой матрице без
дополнительных аксиом; при $p = 1$ совпадение верно безусловно, так как в поле
$0^(-1) = 0$.

#rocq-lemma("faddeev.v", "fl_inv_correct", proof: false)

#rocq-lemma("faddeev.v", "fl_inv1", proof: false)

Исполнимый двойственный `cinv_fl` повторяет ту же рекурренту на слое операций
CoqEAL и уточняет сначала `fl_inv`, а через него и `invmx`:

#rocq-definition("seqmx/riccati_seqmx.v", "cinv_fl")

#rocq-lemma("seqmx/riccati_seqmx.v", "cinv_fl_correct", proof: false)

#rocq-lemma("seqmx/riccati_seqmx.v", "cinv_fl_correct1", proof: false)

Сторона `S in unitmx` берётся из спецификации: `kalman.innov_cov_pd` даёт
обратимость инновационной ковариации. Это не нарушает корректности: уточнение
`refines` свидетельствует, что `cinv` вычисляет именно `invmx` на обратимых
матрицах.

== Структура сборки <sec:extract-build>

Файл `riccati_extract.v` отображает индуктивные типы #Rocq на идиоматичные типы
#OCaml и перечисляет извлекаемые программы:

#rocq-codly(
  rocq-raw(
    ```
    Extract Inductive bool => "bool" [ "true" "false" ].
    Extract Inductive list => "list" [ "[]" "( :: )" ].
    Extract Inductive prod => "( * )" [ "( , )" ].
    Extract Inductive nat => "int" [ "0" "succ" ]
      "(fun fO fS n -> if n=0 then fO () else fS (n-1))".

    Extraction "riccati.ml"
      riccati_step_seqmx predict_cov_seqmx innov_cov_seqmx kalman_gain_seqmx
      update_cov_seqmx alt_update_cov_seqmx ctr_seqmx cinv_fl
      mpow_seqmx obsv_gram_seqmx ctrl_gram_seqmx closed_loop_seqmx.
    ```.text,
  ),
  "riccati_extract.v",
  display-icon: false,
)

Выражение `(rocq.extraction ...)` позволяет извлечь `riccati.ml`, содержащий
`*_seqmx`-программы (шаг Риккати, степень матрицы, грамианы, замкнутый контур).
Программы экспериментов устроены так же, как шаг Риккати; например, матрица
замкнутого контура для эксперимента по устойчивости Шура (раздел
@sec:closed-loop) вычисляется программой:

#rocq-snippet("seqmx/experiments.v", "Definition closed_loop_seqmx ")

Драйвер `driver.ml` подставляет коэффициент `Q.t` (zarith), запускает
извлечённые программы и сохраняет результат в формате `JSON`:

```
dune exec extraction/ocaml/driver.exe -- paper/data
```

Файлы `JSON` фиксируются в репозитории и читаются документом Typst, после чего
отображаются средствами `cetz-plot`. Так рис. @fig:dare-conv (сходимость
траектории Риккати к $P_(s s)$), рис. @fig:riccati-mono (монотонность по
Лёвнеру), грамианы и устойчивость замкнутого контура строятся _непосредственно_
на числах, полученных извлечённым из доказательств алгоритмом.

Все извлекаемые программы доказаны без введения дополнительных предположений
(`Assumptions`).

== Ограничения и обходные приёмы <sec:extract-limits>

+ Стационарное `Pss` невычислимо как предел; вместо него берётся конечная
  итерация `riccati_step` до невязки неподвижной точки.

+ Синтетический прогон фильтра (рис. @fig:kalman-run) по своей природе
  стохастичен: коридор $plus.minus 2 sigma_k$ есть _точная_ извлечённая
  ковариация Риккати, тогда как траектория, измерения и оценка получены посевным
  ГПСЧ и извлечённым усилением Калмана (эта часть представляет собой численную
  симуляцию без статуса верифицированного результата).

+ Параметры модели (`F, G, H, Q, R, P0`) становятся аргументами извлечённых
  функций, что даёт свободу настройки на этапе вызова.

#remark[
  Прямое извлечение через crane остаётся открытой задачей. Реализованный подход
  через CoqEAL не зависит от внешнего экстрактора и даёт формализованное
  соответствие между спецификацией и исполняемым кодом.
]
